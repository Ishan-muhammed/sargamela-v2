import type { Post } from '@/payload-types'

import React from 'react'

export const RelatedPosts: React.FC<{ docs?: Post[] }> = () => {
  // This component is from the template and not used in the fest site
  return null
}
